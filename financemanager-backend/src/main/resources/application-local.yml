spring:
  config:
    activate:
      on-profile: local
  datasource:
    driver-class-name: org.postgresql.Driver
    url: "jdbc:postgresql://localhost:5432/financemanager"
    username: postgres
    password: postgres
  jpa:
    show-sql: true