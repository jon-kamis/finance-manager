spring:
  profiles:
    active: local
  application:
    name: Finance Manager
  jpa:
    database: POSTGRESQL
    properties:
      hibernate:
        default_schema: FMDB
server:
  port: 8080
  servlet:
    context-path: /api

# Error messages
genericInternalServerErrorMessage: An unexpected error has occured